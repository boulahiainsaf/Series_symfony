{% extends 'base.html.twig' %}

{% block tittle %}
  create a new serie

{% endblock %}

{% block body %}
    <div>
        {{ form_start(serieForm) }}
        {{ form_widget(serieForm) }}
        <button>create</button>
        {{ form_end(serieForm) }}
    </div>
{% endblock %}

